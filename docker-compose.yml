x-default-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "3004:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped
    networks:
      - ytmp3-network

  frontend:
    image: ghcr.io/bits-cloud-platform/ytmp3-frontend:latest
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: https://ytmp3.bits.co.id
    env_file:
      - path: .env.production
        required: false
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:3000"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped
    networks:
      - ytmp3-network

  backend:
    image: ghcr.io/bits-cloud-platform/ytmp3-backend:latest
    build: ./backend
    env_file:
      - path: .env.production
        required: false
    volumes:
      - app_data:/app/data
      - app_storage:/app/storage
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://127.0.0.1:8000/openapi.json',timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped
    networks:
      - ytmp3-network

  worker:
    image: ghcr.io/bits-cloud-platform/ytmp3-backend:latest
    build: ./backend
    command: celery -A tasks worker --loglevel=info --concurrency=2
    env_file:
      - path: .env.production
        required: false
    volumes:
      - app_data:/app/data
      - app_storage:/app/storage
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import psutil;exit(0 if any('celery' in ' '.join(p.cmdline()) for p in psutil.process_iter()) else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped
    networks:
      - ytmp3-network

  redis:
    image: redis:latest
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging: *default-logging
    restart: unless-stopped
    networks:
      - ytmp3-network

volumes:
  app_data:
    name: ytmp3_app_data
  app_storage:
    name: ytmp3_app_storage
  redis_data:
    name: ytmp3_redis_data

networks:
  ytmp3-network:
    driver: bridge
